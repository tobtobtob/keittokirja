<h1> Muokkaa reseptiä </h1>
	<div class="form">
		<%= form_tag(recipe_path,:multipart => true, method: 'patch') do %>
			<%= label_tag(:name, "Reseptin nimi") %> </br>
			<%= text_field(:recipe, :name) %> </br>
			<%= label_tag(:description, "Lyhyt kuvaus reseptistäsi") %> </br>
			<%= text_area(:recipe, :description, :size => "40x5") %> </br>
			<%= label_tag(:ingredients, "Ainesosat (erottele rivinvaihdoin)") %> </br>
			<%= text_area(:recipe, :ingredients, :size => "40x20") %> </br>
			<%= label_tag(:directions, "Valmistusohje") %>	</br>
			<%= text_area(:recipe, :directions, :size => "40x20") %> </br>
			<%= label_tag(:photo, "Kuva") %> </br>
			<%= file_field(:recipe, :photo) %> </br> 
			<%= hidden_field(:id, @recipe.id) %>
			<%= submit_tag("Lähetä") %> </br>
		<% end %>
	</div>
